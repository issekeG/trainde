<form method="post" action="{{ path('app_voucher_delete', {'id': voucher.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voucher.id) }}">
    <button class="btn btn-danger mx-5">Delete the item</button>
</form>
